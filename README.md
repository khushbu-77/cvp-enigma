# cvp-enigma

A Flask application for issuing and verifying certificates.

## Setup

1. Install dependencies:
   ```
   pip install -r ../requirements.txt
   ```

2. Run the application:
   ```
   python app.py
   ```

The app will start on http://127.0.0.1:5000/

## Features

- Issue certificates
- Verify certificates by ID